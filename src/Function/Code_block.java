package Function;

/**
 *  Display High Score with custom modification
 * */
public class Code_block {
    /*public  static void main(String args[])
    {
        System.out.println("Code Block !!");
        boolean gameover=true;
        int levelcompleted=8;
        int bonus=1000,score=10000;

        int Fscore=calculatescore(true,10,500,7800);// calling function
        System.out.println("your final score was "+Fscore);
        Fscore=calculatescore(true,12,500,12000);// calling function
        System.out.println("Your final Score Was "+Fscore);
    }
    public  static int calculatescore(boolean gameover,int levelcompleted,int bonus,int score)
    {
        boolean Premiumuser=false;
        boolean lockdownT=true;
                if(lockdownT)
                {
                    System.out.println("You must be check our free premium  Account ");
                }
        if(gameover)//means gameover==true;
        {   int finalscore=score+(levelcompleted*bonus);
            if(Premiumuser)
            {

                return finalscore+200;
            }
            else {
                return finalscore;
            }


        }
        return -1;// -1 is use for in case of error okay !!



    }
*/

    // Challenge time now
   /* public  static void main(String args[])
    {
        int score=1000;
            int pos=CalculatePos(1500);
            DisplayPos("rohit",pos);
            pos=CalculatePos(score);
            DisplayPos("Kajal",pos);
            pos=CalculatePos(400);
            DisplayPos("Sonal",pos);
            pos=CalculatePos(50);
            DisplayPos("Salman",pos);


    }
    public static void DisplayPos(String Pname,int position)
    {
        System.out.println(" Hey "+Pname+" You successfully get "+position);
    }
    public static int CalculatePos(int score)
    {
        if(score<=1500 && score>1000)
        {
            return 1;
        }
        else if(score<=1000 && score>500)
        {
            return 2;
        }else if(score<=500 && score>100)
        {
            return 3;
        }else if(score<=200 && score>100)
        {
            return 4;
        }

        else {
            System.out.println("Sorry !!");
        }
        return -1;
    }*/
}
